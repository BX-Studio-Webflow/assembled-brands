// Environment variables exposed by the Webflow Cloud environment.
// Regenerate with `npm run cf-typegen` after editing wrangler.json bindings.
interface CloudflareEnv {
  NEXT_PUBLIC_API_BASE_URL?: string;
  NEXT_PUBLIC_BASE_PATH?: string;
}
